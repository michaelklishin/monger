language: clojure
sudo: required
lein: lein
dist: xenial
before_script:
  - mongod --version
  - ./bin/ci/before_script.sh
script: lein do clean, javac, test
jdk:
  - oraclejdk8
  - oraclejdk9
services:
  - mongodb
branches:
  only:
    - master
    - 3.0.x-stable
